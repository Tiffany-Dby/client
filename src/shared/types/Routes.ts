enum AppRoutes {
  home = "/",
  dashboardPending = "/pending",
  emailRemove = "/remove",
  dashboardLogs = "/logs",
  settings = "/settings",
  review = "/review",
}

export enum ApiRoutes {
  url = "http://localhost:8000",
  emails = "/emails",
}

export { AppRoutes };
