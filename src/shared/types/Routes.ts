enum AppRoutes {
  home = "/",
  dashboardPending = "/pending",
  dashboardLogs = "/logs",
  settings = "/settings",
}

export enum ApiRoutes {
  url = "http://localhost:8000",
  emails = "/emails",
}

export { AppRoutes };
